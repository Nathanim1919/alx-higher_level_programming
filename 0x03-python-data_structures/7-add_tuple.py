#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    first = if tuple_a[0] else 0 + if tuple_b[0] tuple_b[0] else 0
    second = if tuple_a[1] tuble_a[1] else 0 + if tuple_b[1] tuple_b[1] else 0
    new_tuple = (first, second)
    return new_tuple
